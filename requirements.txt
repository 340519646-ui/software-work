# ============================================================
# 依赖清单（Python ≥ 3.9）
#
# 存储使用标准库 sqlite3，因此**不需要** ORM 依赖。
# 标注「可选」的依赖缺失时，请在 config/config.yaml 中关闭对应开关。
# ============================================================

# ---------- 必选：采集与解析 ----------
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ---------- 必选：数据整理与导出 ----------
pandas>=2.2.0
openpyxl>=3.1.2

# ---------- 必选：配置 ----------
PyYAML>=6.0.1
python-dotenv>=1.0.1

# ---------- 必选：测试 ----------
pytest>=8.0.0
pytest-cov>=5.0.0

# ---------- 可选：门户需要 JS 渲染或脚本化登录 ----------
# 安装后还需执行：playwright install
# playwright>=1.44.0

# ---------- 可选：二级抽取（OpenAI 兼容接口 或 本地 ollama，二选一） ----------
# openai>=1.30.0
# ollama>=0.2.0

# ---------- 可选：图片型分享的 OCR ----------
# 安装后还需系统级安装 tesseract-ocr 与中文语言包
# pytesseract>=0.3.10
# Pillow>=10.2.0
